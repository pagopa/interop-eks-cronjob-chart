cronjob:
  schedule: "0 * * * *"
  image:
    repositoryPrefix: docker.io/library
    repositoryName: busybox
    imagePullPolicy: IfNotPresent
    tag: "1.36.1"
  flywayInitContainer:
    create: true

externalSecrets:
  app:
    create: false
  flywayInitContainer:
    create: true
    secretStoreRef:
      name: cluster-secret-store
      kind: ClusterSecretStore
    data:
      - secretKey: FLYWAY_DB_URL
        remoteRef:
          key: "/apps/test-cronjob/db-url"
