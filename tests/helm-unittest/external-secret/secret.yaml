externalSecrets:
  app:
    create: true
    secretStoreRef:
      name: cluster-secret-store
      kind: ClusterSecretStore
    targetSecret:
      name: test-cronjob
    data:
      - secretKey: APP_USERNAME
        remoteRef:
          key: "/apps/test-cronjob/app-username"
