externalSecrets:
  flywayInitContainer:
    create: true
    secretStoreRef:
      name: cluster-secret-store
      kind: ClusterSecretStore
    targetSecret:
      name: custom-flyway-secret
    data:
      - secretKey: flyway-db-url
        remoteRef:
          key: "/apps/{{ .Values.name }}/db-url"
      - secretKey: flyway-db-user
        remoteRef:
          key: "/apps/{{ .Values.name }}/db-user"
