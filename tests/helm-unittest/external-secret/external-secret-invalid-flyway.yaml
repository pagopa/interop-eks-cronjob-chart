externalSecrets:
  flywayInitContainer:
    create: true
    secretStoreRef:
      name: cluster-secret-store
    data:
      - secretKey: FLYWAY_PASSWORD
        remoteRef:
          key: /apps/test-cronjob/db
          unsupported: value
