# "cronjob.schedule" field missing -> JSON schema violation (cronjob required: [schedule])
#
# Expected error from helm lint / helm template:
#   [ERROR] values.yaml: cronjob: schedule is required
#
# helm template charts/interop-eks-cronjob-chart -f tests/general/failure/schema-missing-schedule.yaml
# Expected: exit code != 0

name: test-cronjob
namespace: test-ns

serviceAccount:
  roleArn: "arn:aws:iam::123456789012:role/test-role"

cronjob:
  schedule: # intentionally omitted
  image:
    repositoryPrefix: my-registry.example.com
    tag: "1.0.0"
  resources:
    requests:
      cpu: "100m"
      memory: "128Mi"
    limits:
      cpu: "200m"
      memory: "256Mi"
  flywayInitContainer:
    create: false
