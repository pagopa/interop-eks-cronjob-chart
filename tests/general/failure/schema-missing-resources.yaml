# "cronjob.resources" field missing -> JSON schema violation (cronjob required: [resources])
#
# Expected error from helm lint / helm template:
#   [ERROR] values.yaml: cronjob: resources is required
#
# helm template charts/interop-eks-cronjob-chart -f tests/general/failure/schema-missing-resources.yaml
# Expected: exit code != 0

name: test-cronjob
namespace: test-ns

serviceAccount:
  roleArn: "arn:aws:iam::123456789012:role/test-role"

cronjob:
  schedule: "0 * * * *"
  image:
    repositoryPrefix: my-registry.example.com
    tag: "1.0.0"
  resources: # intentionally omitted
  flywayInitContainer:
    create: false
